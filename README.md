# iScroll
常用滚动插件

var scrollPanle = document.getElementById('wrap')

    new IScroll(scrollPanle, { 
      scrollbars : true ,
      fadeScrollbars:true,
      scrollY: true,
      click: true,
      mouseWheel: true
    });
